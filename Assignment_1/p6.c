#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("%d is the integer equivalent of A \n", 'A');
    printf("%d is the integer equivalent of B \n", 'B');
    printf("%d is the integer equivalent of C \n", 'C');
    printf("%d is the integer equivalent of a \n", 'a');
    printf("%d is the integer equivalent of b \n", 'b');
    printf("%d is the integer equivalent of c \n", 'c');
    printf("%d is the integer equivalent of * \n", '*');
    printf("%d is the integer equivalent of %% \n", '%');
    printf("%d is the integer equivalent of \\ \n", '/');
    printf("%d is the integer equivalent of blank space \n", ' ');
    return 0;
}
